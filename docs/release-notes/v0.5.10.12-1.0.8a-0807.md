## ✨ 本次更新

**桌宠与 AU 改脸修复版。** 面向玩家的实际修复有三项：切换脸型时桌宠头部消失、桌宠开着却
看不到人，以及 AU 版只切脸型时仪态没有同步、眼睛消失。其余改动在构建流水线与文档内部，
不改变游戏内其它体验。

四体型（base / AU-F / AU-M / AU-A）× 双格式（ZIP + APK），共 8 个产物。

## 🐛 问题修复

- **开着桌宠切换改脸，桌宠的头会消失，并弹出 `base-head.png` 加载失败的红框** —— 框架在挑选
  脸型专属头部底图时，用了一个「问一下图片在不在」的异步查询，却当成同步结果直接用，于是第一次
  渲染就去取了一张并不存在的 `img/face/<脸型>/base-head.png`。本次改成用框架自己在启动阶段
  建好的脸部图片索引来判断：脸型确实自带头部底图就用它，没有就直接退回通用底图。
  **这不是 AU 美化包缺图**（0802 / 0804 的说明把原因写成了 AU 缺素材，那个归因是错的），
  AU 的素材是齐的。侧边栏立绘一直正常，受影响的只有桌宠这块独立画布。

- **桌宠开了却看不到，偶尔一闪就没了** —— 游戏每切换一次段落都会重建页脚，桌宠挂画布用的那个
  容器会被换成一个新的空容器，而框架只在侧边栏刷新时才重新挂载桌宠。切段落不一定触发侧边栏
  刷新，于是桌宠虽然是开着的状态却不再出现。「偶尔一闪」是某些操作刚好触发了刷新。本次让框架
  在每次段落渲染后检查一下：桌宠开着而容器是空的，就走框架自己的侧边栏刷新流程重新挂上。

- **AU 版切换改脸后外观没有同步，不额外选择仪态就没有眼睛** —— 游戏原本假定每个脸型都存在
  一个代码值为 `default` 的默认仪态，因此切换脸型时会把仪态固定设成 `default`。AU 改脸使用的
  实际仪态值是“大眼鼠鼠”“猫猫脸”“Q萌一号”等，没有对应的 `default`，于是只点脸型时会形成
  无效组合，直到再点一个仪态才恢复。本次在角色创建、镜子和作弊页三个换脸入口中，都会自动选择
  新脸型注册的第一个合法仪态并立即刷新模型。此前已经保存下来的无效组合也会在读档时自动修正；
  已经合法的仪态，以及未在 `setup.faceVariantOptions` 中注册仪态的第三方脸型，不会被改动。
  修复会在汉化完成后应用，不再改写汉化器的原始输入；已用隔离 APK 确认开局设置和角色创建保持
  中文，不会因为本次换脸修复退回英文。

## 📦 版本构成

- **游戏本体**：DoL 0.5.10.12 · **汉化**：1.0.8a
- **框架**：[秋枫白桦框架](https://github.com/MaplebirchLeaf/SCML-DOL-maplebirchFramework) 4.1.13
  ＋ [作弊拓展](https://github.com/chris81605/Degrees-of-Lewdity_Cheat_Extended) v1.20
  ＋ [更长遭遇战](https://github.com/MaplebirchLeaf/LongerCombat) 1.0.1
  ＋ [言灵作弊集](https://github.com/MaplebirchLeaf/YanlingCheatCollection) 1.0.1
- **核心 Mod**（点击跳转作者仓库）：
  - [通用战斗美化 UCB](https://github.com/site098/mysterious)
  - [更多恋人](https://github.com/Nephthelana/DoL-More-Love-Interests-Mod) v0.1.7.0
  - [自定义染发](https://github.com/HiddenCirno/DoL-CustomHair)
  - [NPC侧边栏头像](https://github.com/Maenoko/Mae-s-Picvary-NPC-mod) 1.3.2
  - [NPC社交栏头像](https://github.com/Eudemonism00/DOL-NPC-Avatars-Mod) 1.4.1
  - [控制NPC嘴部](https://github.com/Ayndpa/DOL-GuideToMe) v1.1.0
  - [NeoUI](https://github.com/RyaraSUKI/dol-neoui-patch) V1.1.0
  - [D.O.L.I](https://github.com/ArsNativa/Degrees-of-Lewdity-Intelligence)（AI 增强，需玩家自填 API key）
  - [AU美化](https://github.com/AOKIUTAGE/UTAGEsDOL3.0)（仅 AU 三版）
- **旧图片名兼容层**：Legacy-Art-Mods-Compat 1.0.3-plusV1.1（已内置）

## 📥 下载说明

| 文件 | 说明 |
|------|------|
| `...-base-0807` | 基础版，不含 AU 体型模型 |
| `...-au-f-0807` | AU 女性模型 |
| `...-au-m-0807` | AU 男性模型 |
| `...-au-a-0807` | AU 双性模型 |

> 后缀 `.apk` 为安卓版，`.zip` 为浏览器版。安卓端应用名 `DoL XFox`，包名
> `com.vrelnir.dol.xfox`，与原版/汉化版共存，请用游戏内导出存档功能转移进度。

## 🔬 实测范围（请按这个预期使用）

- **AU-F 包已上机实测（MuMu 12 安卓模拟器）**：8 个脸型逐个切换，桌宠头部都正常、没有再出现
  图片加载失败。
- **AU 换脸同步与旧存档修复已在隔离测试应用中实测**：角色创建页逐个点击 8 个真实脸型链接，
  不额外点击仪态，8 次都立即选中合法仪态，人物画布随脸型变化，0 个图片报错、0 个页面或控制台
  错误。另在正常开局后保存一份 `Kiss改脸 + default` 的无效测试存档，再通过游戏存档 API 真实
  读档，加载后自动修正为 `Kiss改脸 + 大眼鼠鼠`，红框未出现、人物画布非空。测试应用使用独立
  包名，不接触正式应用及其存档。相同候选中 `ModI18N 1.0.8a` 主语言为 `zh`，“请选择游戏模式、
  角色创建、体型、脸型、仪态”均保持中文，对应英文设置词没有残留。
- **桌宠切段落自动回来这一项，是在装好的 AU-F 包上验证的**：连续切换段落，桌宠每次都自己回到
  屏幕上，穿着与侧边栏一致，没有出现空白或残留。
- **base / AU-M / AU-A 没有上机**：四版共享补丁前的 DoL 主 HTML 和公共 mod 栈，但本次换脸
  补丁只应用于 AU-F / AU-M / AU-A，base 明确不应用；三个 AU 版又分别使用不同版本的体型模型。
  因此不能把 AU-F 的视觉实测直接当作另外三版的真机结论；另外三版只做了自动构建和各自产物检查。
- **没测到的部分**：长时间游玩过程中的桌宠表现、拖动桌宠后的位置记忆、以及桌宠在战斗等特殊
  界面下的行为，这轮没有专门验证。

## ℹ️ 已知情况说明（非缺陷）

- **AU 版桌宠右侧会露出一条脸部特写的边** —— AU 的桌宠画布上并排画了「全身小人」和「脸部特写」
  两部分，靠一条可调的分割线决定显示到哪。框架给桌宠的分割线默认值偏右，正好切在脸部特写上，
  所以会漏出一条边。
  **解决办法**：「选项 → 模组设置 → 秋枫白桦框架 → 桌宠遮罩分割线」拖到 **0px**，就只显示
  完整的全身小人。想看脸部特写的话往右拖到 128px 可以看到完整特写。
  注意这个滑块和它下面的「特写遮罩分割线」是两回事，后者管的是侧边栏的特写。
  基础版没有这个现象，因为它的桌宠画布只有全身小人。

- **更早的旧存档脸部字段仍有未覆盖边界** —— 本版已经修复能够确定的“当前脸型与仪态不匹配”
  情况；但最初反馈中还出现过 `eyesFacestyle`、`mouthFacestyle`、`eyeColor` 等更老式字段报错，
  当时的问题存档已被删除，无法复现和确定正确迁移值。因此不能保证所有历史版本存档的脸部字段
  都已覆盖。如果仍遇到不同于本次“仪态不同步”的红框，请保留问题存档，不要删档或清空错误记录，
  以便继续定位。

- **同时开「NPC 侧边栏图像显示」+「PC 模型模式」后，有名字的剧情角色（比如萨姆）不显示衣服**
  —— 这些角色在模型系统里没有配套的衣服数据可用，是 NPC 模型系统本身的机制，不是本整合打包
  引入的问题，重新打包也修不了。
  **建议**：只开「NPC 侧边栏图像显示」就能正常看到 NPC 立绘，不要再叠加开启实验性的
  「PC 模型模式」。（两个开关都在「模组设置 → 秋枫白桦框架」里。）

- **云存档连不上** —— 框架自带云存档功能，但作者没有提供公共服务器，需要自己搭建才能用。
  开箱状态下这个功能没有可连的地方，本地存档和导出存档不受影响。

---

> 📖 完整技术细节见 [CHANGELOG.md](https://github.com/XFoxLG/DOL-X/blob/vega/CHANGELOG.md)　·　
> 本仓库非 DoL / 汉化组官方发布渠道，是自用整合包
