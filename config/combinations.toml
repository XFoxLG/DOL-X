# MOD 组合配置

# 稳定自用构建列表：UCB + more-love + custom-spellbook。
# cheatExtended+maplebirch 保留为实验 feature，不进入默认构建。
build_codes = ["24834", "25858", "26882", "28930"]

# 基础版显示名称对应的组合：UCB + more-love + custom-spellbook。
base_code = 24834

# 推荐的组合（优先显示）
recommended = [25858, 26882, 28930]

# 白名单（额外添加的组合，即使不满足规则也会生成）。
# 已改为 build_codes 精确控制，当前不再依赖白名单。
whitelist = []

# 黑名单（排除的组合）
blacklist = []

# Polyfill 配置
[polyfill]
enabled = false
code = 3
